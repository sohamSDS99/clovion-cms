import { ContentAgentScreen } from "@/components/contentagent/ContentAgentScreen";

/** Content Agent — multi-role AI content generation (orchestrator→writer→QA). */
export default function ContentAgentPage() {
  return <ContentAgentScreen />;
}
