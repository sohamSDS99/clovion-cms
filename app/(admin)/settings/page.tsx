import { AiSettings } from "@/components/settings/AiSettings";

/** Settings (P1): AI provider configuration. SOPs live at /sops. */
export default function SettingsPage() {
  return <AiSettings />;
}
