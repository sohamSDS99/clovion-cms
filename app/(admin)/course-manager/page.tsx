import { CourseManagerIndex } from "@/components/courses/CourseManagerIndex";

/** Course manager — all courses (COURSE lessons grouped by courseSlug). */
export default function CourseManagerPage() {
  return <CourseManagerIndex />;
}
